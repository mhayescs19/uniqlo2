import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        "outline-gray": "#c6c6c6",
        "outline-gray-light": "#e3e8f0",
      },
    },
  },
  plugins: [],
};

export default config;
